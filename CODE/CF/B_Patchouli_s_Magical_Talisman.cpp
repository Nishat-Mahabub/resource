#include<bits/stdc++.h>
using namespace std;
#define ll long long
#define fst first
#define sec second
int main(){
    ios::sync_with_stdio(0); cin.tie(0);
    ll t;cin >> t;
    while(t--){
        ll n;cin >> n;
        cout << (1 << n) << endl ;
        // vector<ll> ov,ev;
        // ll data;

        // for(ll i=0;i<n;i++){
        //     cin >> data;
        //     if(n&1) ov.push_back(data);
        //     else ev.push_back(data);
        // }
        // ll e=ev.size();
        //     ll o=ov.size();
        // if(e==0) cout << 0 << endl;
        // else if(o==)
        // else{
            
        //     if(e<=o) cout << e << endl;
        //     else{

        //     }
        // }
    }
    return 0;
}