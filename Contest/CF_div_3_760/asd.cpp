#include<bits/stdc++.h>
using namespace std;
#define ll long long
#define fst first
#define sec second

int maxResult(vector<int>& nums, int k) {
        int ans=0;
        for()
    }

int main(){
    ios::sync_with_stdio(0); cin.tie(0);
    ll t;cin >> t;
    while(t--){
        string s1,s2,s3;
        cin >> s1 >> s2 >> s3;
        cout << isInterleave(s1,s2,s3) << endl;
    }
    return 0;
}